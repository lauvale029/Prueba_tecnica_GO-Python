# Servidor
# ejemplo: 8080
PORT=
# ejemplo: development
ENV=

# Base de datos (Postgres)
# ejemplo: localhost
DB_HOST=
# ejemplo: 5432
DB_PORT=
# ejemplo: mova
DB_USER=
# ejemplo: mova
DB_PASSWORD=
# ejemplo: mova_payments
DB_NAME=
# ejemplo: disable
DB_SSLMODE=
# Construida a partir de las variables anteriores; usada directamente por la app.
# ejemplo: postgres://mova:mova@localhost:5432/mova_payments?sslmode=disable
DATABASE_URL=

# Redis (idempotencia + cache de resumen)
# ejemplo: localhost:6379
REDIS_ADDR=
# ejemplo: (vacío si Redis no tiene password)
REDIS_PASSWORD=
# ejemplo: 0
REDIS_DB=

# Autenticación (JWT)
# nunca uses este ejemplo como secreto real
JWT_SECRET=
# ejemplo: 60
JWT_EXPIRATION_MINUTES=